export interface FooterProps {
  staking?: boolean;
  red?: boolean;
}

export interface FooterItemsProps {
  staking?: boolean;
}

export interface SocialMediaProps {
  staking?: boolean;
  contact?: boolean;
}
